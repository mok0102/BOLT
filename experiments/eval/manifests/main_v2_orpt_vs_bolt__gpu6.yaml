# Milestone-group chunk 6 of main_v2_orpt_vs_bolt.yaml, for parallel
# execution across free GPUs. Milestone in this chunk: 600. See
# main_v2_orpt_vs_bolt.yaml for the full comparison this was split from, and
# for why MTBO/OptFormer/POGPE/SGPE/LLAMBO rows aren't included here yet
# (not trained at this schedule).
models:
- arm: BOLT
  milestone: 600
  run_dir: runs/peptide_main_bolt_v2
  checkpoint_dir: runs/peptide_main_bolt_v2/checkpoints/BOLT-600/epoch_4
- arm: ORPT-H1
  milestone: 600
  run_dir: runs/peptide_main_orpt_h1_v2
  checkpoint_dir: runs/peptide_main_orpt_h1_v2/checkpoints/ORPT-600/epoch_0
